<section class="panel">
  <h1 class="title">애니메이션 허브</h1>
  <p class="muted">기존 React 화면 흐름을 Rails 서버 렌더링으로 옮긴 웹 앱입니다.</p>
  <div class="row" style="margin-top: 14px;">
    <a class="btn" href="<%= anime_path %>">애니 둘러보기</a>
    <a class="btn alt" href="<%= news_path %>">최신 뉴스</a>
    <a class="btn alt" href="<%= quotes_path %>">명대사 모음</a>
  </div>
</section>

<section style="margin-top: 14px;" class="panel">
  <div class="row">
    <h2 class="title" style="font-size: 1.2rem; margin: 0;">최신 애니</h2>
    <a class="muted" href="<%= anime_path %>">전체 보기</a>
  </div>
  <div class="grid" style="margin-top: 10px;">
    <% @latest_anime.each do |anime| %>
      <article class="card">
        <h3 style="margin: 0 0 8px;"><a href="<%= anime_page_path(anime) %>"><%= anime.title_romaji.presence || anime.title_english || anime.title_native || 'Untitled' %></a></h3>
        <p class="meta"><%= anime.season.to_s.upcase %> <%= anime.season_year %></p>
      </article>
    <% end %>
  </div>
</section>

<section style="margin-top: 14px;" class="grid">
  <div class="panel">
    <h2 class="title" style="font-size: 1.1rem;">최신 뉴스</h2>
    <% @latest_news.each do |article| %>
      <p style="margin: 8px 0;">
        <a href="<%= article.url %>" target="_blank" rel="noopener"><%= article.title %></a>
      </p>
    <% end %>
  </div>
  <div class="panel">
    <h2 class="title" style="font-size: 1.1rem;">최근 명대사</h2>
    <% @latest_quotes.each do |quote| %>
      <p style="margin: 8px 0;">
        “<%= truncate(quote.quote_text, length: 90) %>”
        <span class="meta">- <%= quote.character_name %></span>
      </p>
    <% end %>
  </div>
</section>
